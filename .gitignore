# MySQL database dump / backups
*.sql
*.dump
dump/

# MySQL logs
*.log

# OS files
.DS_Store
Thumbs.db

# Environment / config files (contains DB credentials)
.env
config/*.env
config/db_config.js
config/database.yml

# Node.js dependencies (if any)
node_modules/

# Build outputs
dist/
build/



# .env → protects your database credentials. ✅

# node_modules/ → avoids pushing bulky dependency folders. ✅

# *.sql, *.dump, dump/ → avoids backup/dump files. ✅

# .DS_Store, Thumbs.db → ignores OS junk files. ✅

# config/*.env, config/db_config.js, config/database.yml → protects additional config files. ✅

# dist/, build/ → ignores build output. ✅